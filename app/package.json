{
  "name": "simple-node-api",
  "version": "1.0.0",
  "description": "Minimal Node.js REST API for testing",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "echo \"No lint configured\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
